# Daily repo lab — 2026-08-09 (cron 06:00 UTC)

## Knowledge ingest (10 notes)

Branch: `cursor/daily-ingest-2026-08-09` · commit `26490a7` (local; push 403)

| id | title |
| --- | --- |
| `mcp-transport-stdio-vs-http` | MCP transport — stdio vs Streamable HTTP |
| `agent-workflow-durability` | Durable agent workflows with WorkflowAgent |
| `git-worktree-agent-isolation` | Git worktree isolation for parallel agents |
| `llm-gateway-failover` | LLM gateway failover and multi-provider routing |
| `agent-streaming-ui` | Streaming agent output to UI (tool + text chunks) |
| `browser-verification-agents` | Browser verification for agent-built features |
| `mcp-resources-vs-tools` | MCP resources vs tools — when to use which |
| `plan-vs-agent-mode` | Plan mode vs Agent mode in Cursor |
| `rag-chunking-strategies` | RAG chunking strategies for agent retrieval |
| `cloud-agent-environment-builds` | Cursor Cloud Agent environments and Builds |

`npm run check`: pass (37 entries, 0 warnings)

## Repo iterate (10 repos)

| # | repo | improvement | SHA | push |
| --- | --- | --- | --- | --- |
| 1 | otel-sieve | Document `-h`/`-v` in CLI usage | `095cc5c` | fail |
| 2 | agent-ledger | Exit 2 for missing session/policy files | `2289d0c` | fail |
| 3 | pluginport | Clear error when plugin directory missing | `fad7cde` | fail |
| 4 | diffguard | Reuse shared severity helpers in CLI | `362f2d7` | fail |
| 5 | signal-drift | Fix sessionStorage key prefix + legacy migration | `77a4024` | fail |
| 6 | mcplint | Align text footer with `--fail-on` threshold | `8b571ef` | fail |
| 7 | agentbrief | Reject invalid `--budget` at parse time | `3cf4b92` | fail |
| 8 | orbitdesk | Resume AudioContext before chime (Safari) | `6f368ec` | fail |
| 9 | ruleradar | Dedupe `formatSigned` into tokens module | `79b16cd` | fail |
| 10 | helpgate | Allow `--help-cmd -h` for short-help CLIs | `9dddae1` | fail |

## Failures

- All pushes to `piyatat/*` except `daily-idea-lab` returned **403** (`cursor[bot]` lacks write access). Local commits verified on VM.
