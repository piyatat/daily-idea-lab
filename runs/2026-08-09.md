# Run — 2026-08-09

## Scout highlights

- **ChainDrop (critical, live):** Self-propagating npm worm since ~2026-08-04; 400–1300+ packages; preinstall + Bun payload; steals npm/GitHub/cloud/AI tokens; abuses trusted publishing/provenance; plants cross-linked persistence in `.claude/settings.json` (SessionStart) and `.vscode/tasks.json` (`folderOpen`). npm package pulls do **not** clear reinfection. Host `gh-token-monitor` can fire attacker payload on token revoke.
- **EU AI Act Art.50:** Transparency obligations enforceable since 2026-08-02 (chat disclosure, synthetic marking, deepfake labels). High-risk delayed; transparency is not.
- **Claude Code cost rage:** Cache reads dominate quota; silent session resumes re-fire SessionStart + MCP instructions; 200K extended-context cliff; `/cost` undercounts invoice.
- **Agent continuity / verify-done:** Multi-agent session managers launching; “done” claim verification tools proliferating (crowded).
- **Sandbox / permission floors:** UnYOLO on HN; Codex/Claude sandbox hardening posts; Grok Build permission debates — demand for fail-closed “minimum floor” auditors.
- **Portfolio avoid:** pluginport, agent-ledger, otel-sieve, diffguard, mcplint, dangertape, ruleradar, agentbrief, helpgate, lockplain, invoicekit, orbitdesk, signal-drift, redline, agent-knowledge, Agent Plugins validators.

### Candidate shortlist (debate)

1. **hardenfloor** (trend scout) — fail-closed static auditor for coding-agent permission/sandbox floor configs
2. **reinfect** (pain-point scout) — post-remediation ChainDrop reinfection + revoke-booby-trap scanner
3. Also considered (not finalists): egressmap, workfence, hookbypass, agentblame, art50inv, prefixburn

## Debate outcome

- **Winner:** hardenfloor
- **Pitch:** Fail-closed CLI that audits Claude + Codex sandbox/permission configs against a minimum security floor and exits non-zero on YOLO/bypass gaps.
- **Why it beat reinfect:**
  - One-session MVP is tighter: parse known config schemas + deterministic rules vs IOC hash DB + home-dir monitors + commit heuristics.
  - Fixture-testable without malware artifacts; durable beyond the ChainDrop news cycle.
  - Novel vs last 3 days (otel-sieve / pluginport / AgentLedger); clearer gap than a worm-specific scanner that ages with IOCs.
- **Overlap:** clear (adjacent-to dangertape/ruleradar only in “agent safety” theme; different artifact — static permission floors, not replay or rule radar)
- **Action:** stage new project under `projects/hardenfloor/` in daily-idea-lab (repo split later)
- **Bin / tests:** `hardenfloor`; fixture dirs for safe vs YOLO Claude/Codex configs; `node --test`

## Result

- **Repo:** `projects/hardenfloor/` (to be implemented)
- **Commit / PR:** debate note only in this turn
- **Notes:** Debate converged on hardenfloor. reinfect deferred (high urgency, messier day-1 surface).
