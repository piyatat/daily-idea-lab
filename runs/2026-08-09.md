# Daily repo lab — 2026-08-09 (cron 21:00 UTC)

## Knowledge ingest (10 gap-fill notes)

Corpus: `piyatat/agent-knowledge` — local commit `0fe7b08` (37 entries, `npm run check` 0 warnings).

| id | title |
| --- | --- |
| `workflow-checkpointing` | Workflow checkpointing and durable agent execution |
| `llm-provider-failover` | LLM provider failover and multi-model routing |
| `parallel-tool-calls` | Parallel tool calls — runtime and dependency hygiene |
| `agent-guardrails-validation` | Agent guardrails — validate before side effects |
| `agent-audit-trail` | Agent audit trails for compliance and forensics |
| `agent-filesystem-boundaries` | Filesystem and git write boundaries for coding agents |
| `memory-pointer-large-outputs` | Memory pointer pattern for large tool outputs |
| `tool-result-poisoning` | Tool result poisoning (indirect prompt injection) |
| `context-window-overflow` | Context window overflow failure modes |
| `silent-tool-truncation` | Silent tool output truncation |

**Push:** agent-knowledge → 403 (local only)

## Repo iterate (10 improvements)

| repo | change | SHA | push |
| --- | --- | --- | --- |
| signal-drift | Document R key reset in README and keyboard cheatsheet | `eb0a164` | 403 |
| invoicekit | Add `npm run typecheck` for CI smoke | `065424c` | 403 |
| agentbrief | Add `npm test` smoke for `--help` and `agent --json` | `520064e` | 403 |
| redline | Accept top-level `--help` / `-h` without subcommand | `8646b26` | 403 |
| otel-sieve | Accept `-V` alias for `--version` | `a5d3918` | 403 |
| agent-ledger | Hint `agentledger --help` on unknown flag errors | `d4e8ee6` | 403 |
| pluginport | Clear error when plugin dir missing; add fixture `npm test` | `23d98db` | 403 |
| diffguard | Add `-C` alias for `--cwd` | `471d601` | 403 |
| mcplint | Document `--only` rule filter in README | `a03d430` | 403 |
| helpgate | Add `npm test` alias for `test:fixtures` | `ca5c7d3` | 403 |

## Failures / follow-up

- All `piyatat/*` product repos and `agent-knowledge` reject `cursor[bot]` push (403). Commits exist locally on each repo's `main`; merge manually or expand token repo permissions.
- `redline` Swift build not verified on this VM (no `swift` toolchain); change is a small CLI entry-point guard.
- This run summary pushed to `daily-idea-lab` only.
