# Run — 2026-08-21

## Scout highlights

- **Malicious Rust crate arrayref (HN #1):** build-time payload in `build.rs` via bait `proc-macro1` build-dependency; crates.io deleted v0.3.10 without yank flag; `cargo audit` gap.
- **GitHub Aug 17 outage:** client retry loop amplified recovery traffic ~10× (VS Code Copilot token service).
- **AI scope creep (Reddit):** Opus 5 over-engineering backlash — agents expand beyond task boundaries.
- **Agent infra mainstream:** cross-agent skills, MCP servers (Chrome DevTools MCP, Desktop Commander), Slack Code launch (Aug 20).
- **Other HN:** small native web tricks, Kagi paywall filter, Codex AWS Bedrock 10× charge bug.

## Debate outcome

Three scouts proposed 9 candidates across supply-chain, reliability, and agent-workflow lanes:

| Candidate | Scout | Score | Notes |
|-----------|-------|-------|-------|
| **buildref** | security | 9/10 | Rust build-script bait linter — arrayref hook |
| retrybudget / retrystorm | reliability | 9/7 | Retry amplification from GitHub outage |
| scopefence / scopesnap | workflow | 9/8 | Agent scope creep vs approved plan |
| mcpprobe | reliability | 7/10 | Runtime MCP health — overlaps mcplint |
| plancreep / writeaudit | workflow | 7–8/10 | Plan mutation / JSONL write audit |

**Winner:** buildref

**Why:** arrayref is on the HN front page *today* with a reproducible supply-chain failure mode (`cargo audit` + yank semantics both missed it). Extends the portfolio supply-chain lane (`trapdeck` → npm, `buildref` → Rust build-time) with deterministic plain-text parsing and zero network deps. Beats retrybudget (outage is 4 days old, noisier heuristics) and scopefence (adjacent to diffguard boundary enforcement, fuzzier CI story).

**Action:** create new project (no overlapping piyatat repo)

**Overlap check:** trapdeck = npm lifecycle bait; hardenfloor = agent permission floors; mcplint/pluginport = MCP/plugin manifests; diffguard = git diff *risk* not build surfaces. No Rust/Cargo build-script scanner exists in portfolio.

## Result

- **Repo:** piyatat/daily-idea-lab (`projects/buildref/`)
- **Branch:** cursor/daily-web-trend-projects-96fb
- **Shipped:** zero-dep Node CLI — `buildref scan|rules|explain`, 5 rules, 4 fixture trees, `--json`, `--fail-on`
- **Rules:** `BUILD_NET_FETCH`, `BUILD_SHELL_EXEC`, `BUILD_ENV_EXFIL`, `LOCK_BAIT_BUILD_DEP`, `PROC_MACRO_BUILD_CHAIN`
- **Verified:** 12/12 tests, syntax checks pass
