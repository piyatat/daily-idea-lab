# Run — 2026-08-07

## Scout highlights

- **Agent Plugins 1.0.0** launched Aug 6, 2026 (AWS, Cursor, GitHub, Microsoft, OpenAI, Vercel TSC; Google joined Aug 7)
- Portable format: root `plugin.json` + `skills/` + `mcp.json` — clients validate independently; one bad MCP entry must not break skills
- **Ecosystem gap:** client-specific validators exist (Hanko, claude-plugin-validate for Claude Code) but **no cross-vendor Agent Plugins package linter**; MCP scanners (mcp-verify, mcpscanner) target runtime servers, not the plugin bundle contract
- **HN/security trends:** MCP supply chain, permission control, auditability; spec mandates path containment, no secrets in headers/env, HTTPS for remote MCP
- Spec normative checks beyond JSON Schema: filesystem containment, `${PLUGIN_ROOT}`/`${PLUGIN_DATA}` rules, transport portability matrix (stdio + streamable-http required floor; sse optional)

## Debate outcome

- **Winner:** pluginport — Agent Plugins 1.0.0 conformance & portability linter
- **Runner-up:** flowaudit (agent transcript read→sink audit; deferred — harder v0, adjacent to yesterday's AgentLedger)
- **Merged:** skillgate SKILL.md slop checks folded into pluginport `skills` rule
- **Why:** Ships in hours against published schemas; fills the exact validation/security/portability gap the TSC left to clients; complements mcplint (MCP runtime security)
- **Action:** create new repo `piyatat/pluginport`

## Result

- **Repo:** staged at `projects/pluginport/` in `piyatat/daily-idea-lab` (GitHub token cannot create new repos under `piyatat`; split to standalone repo manually)
- **Commit / PR:** branch `cursor/daily-idea-lab-project-7530`
- **Notes:** Working CLI — `pluginport check` (schema, containment, secrets, skills, portability) + `pluginport compat` (cross-client matrix). Fixtures: `fixtures/valid-plugin`, `fixtures/invalid-plugin`.
