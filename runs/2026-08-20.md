# Run — 2026-08-20

## Scout highlights

- **Slack Code launch (Aug 20)** — team agent coding in Slack channels; coordination layer not execution
- **Cursor Origin (Aug 17)** — agent-scale code hosting, PR velocity
- **OpenAI Assistants API shutdown Aug 26** — 6 days left; no auto migration; threads don't migrate; DEV article calls for CI gate scanner
- **Agent control plane pain** — HN Ask HN about mission control for multi-agent local work
- **Context compression** — headroom, context-mode for token reduction
- **Model routing cache invalidation** — mid-session model switch wipes prompt cache
- **Layer-isolated eval regressions** — aggregate metrics hide 25–91pt layer failures
- **Yesterday shipped prnoise** — PR prose AI;DR linter

## Debate outcome

### A. assistscan

**Strengths:** Strongest timeliness — Aug 26 hard cutoff in 6 days; DEV article explicitly calls for a CI gate scanner; zero-dep repo grep is one-session shippable; pattern rules (`beta.assistants`, `beta.threads`, `/v1/threads`) are fully deterministic; no portfolio overlap.

**Weaknesses:** Demand cliff after shutdown week; pattern maintenance as SDKs rename endpoints; cannot verify runtime behavior, only call-site presence.

### B. cachebreak

**Strengths:** Real silent-cost pain from mid-session model switches; JSONL scanning reuses portfolio patterns; durable as long as model routing exists.

**Weaknesses:** Weakest launch-day tie-in (HN debate, no deadline); overlaps agent-ledger (cost attribution) and seqtax (JSONL session lint); harder to prove ROI without provider billing data.

### C. slackhandoff

**Strengths:** Same-day Slack Code launch hook; fills team-visibility gap for channel-native agent work; no direct portfolio overlap.

**Weaknesses:** Requires Slack export JSON (not universal in v1); handoff field conventions are fuzzy; less CI-friendly than repo scanners; coordination-layer lint may lag actual Slack Code adoption.

### D. planlint

**Strengths:** Highly shippable markdown rules; durable; planning-with-files is mainstream.

**Weaknesses:** No Aug 20 deadline or launch tie-in; conceptual adjacency to yesterday's prnoise (prose/doc linting); crowded planning-tool space.

### E. layereval

**Strengths:** Strong deterministic math on eval JSON; durable for agent eval teams; novel aggregate-vs-layer regression detection.

**Weaknesses:** June paper signal, not today-specific; narrower audience than migration scanners; some adjacency to greensweep (agent success signals).

### Ranking (criteria-weighted)

| Candidate   | Timeliness | Shippability | Gap | Deterministic | Durable | Total |
|-------------|------------|--------------|-----|---------------|---------|-------|
| assistscan  | 5          | 5            | 5   | 5             | 3       | **23** |
| slackhandoff| 5          | 3            | 5   | 3             | 4       | 20 |
| layereval   | 3          | 4            | 4   | 5             | 5       | 21 |
| cachebreak  | 3          | 4            | 3   | 4             | 5       | 19 |
| planlint    | 3          | 5            | 3   | 4             | 5       | 20 |

- **Winner:** assistscan
- **Why:** Only candidate combining a hard 6-day deadline, explicit external call for a CI scanner, one-session repo-grep MVP, clean portfolio gap, and fully deterministic fixture tests. Slack Code launch makes slackhandoff second but fuzzier conventions and weaker CI story lose the tie-break.
- **Action:** create new project (no overlapping piyatat repo)

## Result

- **Repo:** assistscan (new — stage under `projects/assistscan/` in daily-idea-lab)
- **Commit / PR:** debate only — implementation pending
- **Notes:** Pitch — zero-dep Node CLI that scans repos for OpenAI Assistants API call sites, prints days-until-shutdown (Aug 26), emits JSON report, and supports `--fail-on` for CI gates before the hard cutoff.
