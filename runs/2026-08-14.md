# Run — 2026-08-14

## Scout highlights

- **DeepSeek Harness (Aug 13):** Open-source Cordis plugin runtime; append-only session log; everything (model adapter, tool registry, agent loop) swappable. Developer preview with breaking changes.
- **Bullet YC S26 (Aug 13):** Speed-first coding agent — parallel independent tool calls, duplicate interception, stuck-loop detection; claims 95.8% SWE-bench @ 119s; "sequential tool calls are the hidden tax."
- **Zed Delta (Aug 12):** Multiplayer agent review app; DeltaDB links conversation + worktree; anchored comments survive rewrites; team pickup of agent threads.
- **GPT-5.6 routing:** Sol/Terra/Luna tiers + programmatic tool calling + multi-agent beta; OpenAI docs warn routing must be task-specific, not generic.
- **Gemini 3.7 Flash (Aug 13):** Agent workhorse GA; Antigravity default model swapped under same agent string — teams must log resolved checkpoint.
- **Muse Glimmer 30B (Aug 10):** Local always-on agents on 24–32GB GPU; pilot guidance stresses tool-call error rate and failure recovery on your scaffold.

## Debate outcome

- **Winner:** seqtax
- **Why:** Bullet made sequential tool-call latency the week's clearest infrastructure pain; zero-dep JSONL linter is one-day shippable with deterministic fixtures; no overlap with greensweep (silent success), dangertape (replay), or agent-ledger (cost).
- **Action:** create new repo

## Result

- **Repo:** (pending implementation)
- **Commit / PR:** —
- **Notes:** Scout JSON proposal recorded below.

### Scout proposal (JSON)

```json
{
  "name": "seqtax",
  "one_line_pitch": "Quantify sequential tool-call tax in agent session JSONL — missed parallel reads, duplicate calls, and stuck loops.",
  "pain_point": "Most harnesses still execute independent tool calls one-at-a-time, inflating wall-clock latency 50–70%; duplicate calls and retry loops add hidden seconds Bullet explicitly intercepts.",
  "why_today": "Bullet (YC S26, Aug 13) launched on speed — parallel execution and duplicate/loop interception as core differentiators. DeepSeek Harness and GPT-5.6 programmatic tool calling push tighter tool pipelines, but teams have no offline linter to measure wasted sequential rounds in their own traces.",
  "mvp_scope": [
    "Parse JSONL tool events (tool name, args fingerprint, turn_id, timestamp, optional touched paths) from common agent export shapes",
    "Emit findings: parallelizable groups (independent reads in the same turn), duplicate identical calls, stuck loops (≥3 same tool+args), and estimated round-trip savings",
    "CLI: `seqtax scan trace.jsonl` with `--json`, `--fail-on parallel|dup|loop`, exit 0 (clean) / 1 (findings) / 2 (parse error)"
  ],
  "test_strategy": "Fixture JSONL traces with golden JSON reports: sequential-three-reads (1 parallel group, 2 saved round-trips), duplicate-rg (dup hit), edit-fail-loop (stuck loop), clean trace (exit 0). Node built-in test runner only; no network or live API.",
  "overlap_check": "Not dangertape (post-hoc replay), agent-ledger (cost), ghostroster (subagent lifecycle), greensweep (silent-green receipts), ops-canary (heartbeats), otel-sieve (OTLP), diffguard (git diffs), mcplint/pluginport (MCP/plugin manifests), hardenfloor (permissions), trapdeck (npm bait), ruleradar (rules token tax), agentbrief (context briefs). Complements speed-focused harness tuning.",
  "score_1_10": 9
}
```
