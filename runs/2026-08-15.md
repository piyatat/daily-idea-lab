# Run — 2026-08-15

## Scout highlights

- **Validation bottleneck** is the dominant 2026 dev pain: AI generates code faster than teams can review; trust in AI output down to 29%; reviewers report 2–3× PR volume with less context.
- **DeepSeek Harness** (`dsh`) launched Aug 13 — MIT, Cordis plugin architecture, 100k+ GitHub stars; `dsh-plugin` community exploding.
- **Toast 1** (Mixedbread search agent) hit HN today — RAG/search sub-agent cost/quality frontier; embedding/search stack shifting open-weight.
- **HN surging:** agents, kimi, claude, codex, agentic, local models, open-weight AI "Kubernetes moment."
- **Security/governance:** agent lifecycle risks, MCP supply chain still elevated.

## Debate outcome

Four scouts proposed:

| Codename | Idea | Verdict |
| --- | --- | --- |
| **dsplug** | dsh/Cordis `cordis.patch.yml` + manifest linter | **Winner** |
| **signrift** | Keel-style export signature drift rule for diffguard | Strong pain, but extends existing diffguard (update path) |
| **toastbench** | Fixture RAG recall@k for embedding swaps | Weaker hook — Toast 1 is a search agent, not pure embeddings |
| **validgate** | Pre-merge AI code validation checklist | Overlaps greensweep/hardenfloor themes |

- **Winner:** dsplug — DeepSeek Harness plugin/bundle linter
- **Why:** Same-week launch urgency, clear gap vs pluginport (Agent Plugins 1.0.0 ≠ Cordis/dsh), zero-dep one-session CLI with deterministic fixtures, complements agent-infra portfolio.
- **Action:** create new project in daily-idea-lab (`projects/dsplug/`)

## Result

- **Repo:** `piyatat/daily-idea-lab` → `projects/dsplug/`
- **Shipped:** zero-dep Node CLI — `dsplug check|patch|stack|rules`, 10 rules, JSON/`--fail-on` output, fixtures + 14 tests
- **Overlap check:** no piyatat repo covers dsh Cordis patches; pluginport is a different spec; diffguard has AI triage but no harness plugin linting
- **Verified:** `npm run check`, 14/14 tests, CLI smoke on fixtures
