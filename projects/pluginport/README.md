# pluginport — moved

This project now lives in its own repository:

**https://github.com/piyatat/pluginport**

History was split out of `projects/pluginport` on branch `cursor/daily-idea-lab-project-7530`.
