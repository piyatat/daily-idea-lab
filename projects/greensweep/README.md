# greensweep — moved

This project now lives in its own repository:

**https://github.com/piyatat/greensweep**

History was split out of `projects/greensweep` on branch `cursor/daily-web-trend-projects-996b`.
