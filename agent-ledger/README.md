# agent-ledger — moved

This project now lives in its own repository:

**https://github.com/piyatat/agent-ledger**

History was split out of `agent-ledger/` on branch `cursor/daily-idea-lab-project-1b2b`.
